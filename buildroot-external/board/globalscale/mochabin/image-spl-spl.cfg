image spl.img {
	size = ${BOOT_SPL_SIZE}

	hdimage {
		partition-table-type = "none"
		fill = "yes"
	}

	partition uboot {
		in-partition-table = "no"
		image = "flash-image.bin"
		offset = 0
		size = 2M
	}
}
